# Pipeline for computing biophysical metrics for monomeric proteins

## Organization of code

* `scripts/` : is a directory containing all code in the scoring pipeline
* `data/` : contains data for analysis
